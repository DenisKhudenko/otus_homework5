
&НаКлиенте
Процедура ЗаполнитьИменаРеквизитов(Команда)
	ЗаполнитьИменаРеквизитовНаСервере();
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьИменаРеквизитовНаСервере()
	
	МассивРеквизитов = Новый Массив();
	МассивРеквизитов = Документы.Документ.ПолучитьРеквизитыДокумента(МассивРеквизитов);
	Для Каждого ЭлементМассива Из МассивРеквизитов Цикл
		НоваяСтрока = ТаблицаРеквизитов.Добавить();
		НоваяСтрока.ИмяРеквизита = ЭлементМассива;	
	КонецЦикла;	
	
КонецПроцедуры
